import { snippetsManager } from '../../../../../../helpers/index.js';
import BaseComponent from './base-component.json' with { type: 'json' };

const ComponentSnippets = snippetsManager([BaseComponent]);

export default ComponentSnippets;
