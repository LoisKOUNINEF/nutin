// used in tools/generators to generate views/components JSON files

/**
 * @typedef {'en' | 'fr'} Language
 */
/** @type {readonly Language[]} */
export const LANGUAGES = /** @type {const} */ (['en', 'fr']);

/** @type {Language} */
export const DEFAULT_LANGUAGE = /** @type {const} */ ('en');