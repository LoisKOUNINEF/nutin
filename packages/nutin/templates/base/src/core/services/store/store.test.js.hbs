import { AppStore } from '#root/dist/src/core/services/index.js'

describe('store', () => {
  it('should set & get values properly', () => {
    AppStore.set('foo', 123);
    expect(AppStore.get('foo')).toBe(123)
  });
})