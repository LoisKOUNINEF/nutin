import { RegisterGlobalsOptions } from '../core/index.js';

// Components mounted outside <main id="app"> that persist across every route
// (e.g. a header/footer), passed to registerGlobals() in main.ts. When SEO files are
// generated, they're also prerendered into each route's static HTML.
//
// import { HeaderComponent, FooterComponent } from './components/index.js';
//
// export const appGlobals: RegisterGlobalsOptions = {
//   before: [{ component: HeaderComponent, id: 'header' }],
//   after: [{ component: FooterComponent, id: 'footer' }],
// };
export const appGlobals: RegisterGlobalsOptions = {};
