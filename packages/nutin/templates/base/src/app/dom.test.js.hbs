describe('App container', () => {
  it('should be defined', () => {
    const appContainer = document.getElementById('app');
    expect(appContainer).toBeDefined();
  });
  it('should have title', () => {
    expect(document.title).toEqual('{{projectName}}');
  });
});
