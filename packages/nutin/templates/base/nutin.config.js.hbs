export default {
  // Nutin features
  inlineTemplates: false,
  i18n: false,
  tailwind: false,

  // Components / Views scaffolding
  generator: {
    generateStylesheet: false, // generates .scss alongside components / views
    generateLocales: false, // generates locales/*.json alongside components / views - With i18n enabled.
    generateTest: false, // generates .test.js alongside components / views / services - With testinNutin.includeApp enabled.
  },

  // Build pipeline
  builder: {
    generateSEO: true,
    logLevel: 0, // 0 = default; 1 = verbose; 2 = debug.
    sass: {
      paths: [ 
        'base', 
      ], // in styles/
    },
    esbuild: {
      bundle: true,
      minify: true,
      sourcemap: false,
      target: ['es2015'],
      drop: ['console', 'debugger'],
    },
    compression: {
      gzip: true,
      // BROTLI OPTIONAL
      // brotli: false,
    },
  },

  // Testing toolkit
  // `npm run testin-nutin`
  testinNutin: {
    includeFramework: true,
    includeTools: false,
    includeApp: false,
    verbose: false,
    coverage: {
      enabled: false,         // `enabled: true` is equivalent to `npm run testin-nutin:coverage`
      treshold: 95,           // exits with code 1 if global branches, functions or lines global coverage is under treshold
      reportUncovered: true,  // generates a .md file in coverage/ 
    },
    jsdomOptions: {
      runScripts: false,     // or true ("dangerously") if needed
      resources: false,      // or true ("usable") if needed
      freezeGlobals: false,
      pretendToBeVisual: true,
    },
  },
}
