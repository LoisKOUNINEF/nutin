export default {
  // Nutin features
  inlineTemplates: false,
  i18n: false, // Add languages in config/languages.json after enabling i18n
  tailwind: false,

  generateSEOFiles: false, // Consumes config/seo.json to generate per-route static HTML, robots.txt and sitemap.xml.

  // Components / Views scaffolding
  generator: {
    generateStylesheet: false, // generates .scss alongside components / views
    generateLocales: false, // generates locales/*.json alongside components / views - With i18n enabled.
    generateTest: false, // generates .test.js alongside components / views / services - With testinNutin.includeApp enabled.
  },

  // Build pipeline
  builder: {
    logLevel: 0, // 0 = default; 1 = verbose; 2 = debug.
    sass: {
      paths: [ 
        'base', 
      ], // in styles/
    },
    esbuild: {
      bundle: true,
      minify: true,
      sourcemap: false,
      target: ['es2015'],
      drop: ['console', 'debugger'],
    },
    compression: {
      gzip: true,
      // BROTLI OPTIONAL
      // brotli: false,
    },
  },

  // Testing toolkit
  // `{{packageManager}} run testin-nutin`
  testinNutin: {
    includeFramework: true,  // test Nutin source - src/core and src/libs
    includeTools: false,     // test tools/ (builder, testin-nutin, etc.)
    includeApp: false,       // enable to use testin-nutin for application tests
    verbose: false,          // log test suites and individual `it` tests
    coverage: {
      enabled: false,        // include coverage in the normal test command
      threshold: 95,         // exit with code 1 if any global coverage metric falls below the threshold
      reportUncovered: true, // generates an .md report of uncovered lines, functions and branches in coverage/
    },
    jsdomOptions: {
      runScripts: false,     // or true ("dangerously") to execute scripts in the DOM
      resources: false,      // or true ("usable") to load external resources
      freezeGlobals: false,
      pretendToBeVisual: true,
    },
  },
}
