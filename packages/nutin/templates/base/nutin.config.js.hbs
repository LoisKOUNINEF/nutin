export default {
  inlineTemplates: false,
  i18n: true,
  tailwind: false,
  generator: {
    generateLocales: false, // generates locales/*.json for components / views
    generateStylesheet: false, // generates .scss for components / views
  },
  builder: {
    generateSEO: true,
    sass: {
      paths: [ 
        'base', 
      ], // in styles/
    },
    esbuild: {
        bundle: true,
        minify: true,
        sourcemap: true,
        drop: ['console', 'debugger'],
        platform: 'browser',
        format: 'esm',
        target: ['es2015'],
        legalComments: 'none',
        loader: {
          '.json': 'json',
        },
        define: {
          'process.env.NODE_ENV': '"production"',
        },
    },
    compression: {
      gzip: true,
      // BROTLI OPTIONAL
      // brotli: false,
    },
    logLevel: 0, // 0 = logs start / finish; 1 = logs steps; 2 = logs all.
  },
  {{#if testinNutin}}
  testinNutin: {
      origins: [
      'src/app',
      // 'src/core', // Nutin core logic - uncomment to test your own modifications
    ],
    jsdomOptions: {
      runScripts: false,     // or true ("dangerously") if needed
      resources: false,      // or true ("usable") if needed
      freezeGlobals: false,
      pretendToBeVisual: true,
    },
    verbose: false,
  },
  {{/if}}
}
