export default {
  // Nutin features
  inlineTemplates: false,
  i18n: false,
  tailwind: false,

  // Components / Views scaffolding
  generator: {
    generateLocales: false, // generates locales/*.json alongside components / views
    generateStylesheet: false, // generates .scss alongside components / views
  },

  // Build pipeline
  builder: {
    generateSEO: true,
    sass: {
      paths: [ 
        'base', 
      ], // in styles/
    },
    esbuild: {
        bundle: true,
        minify: true,
        sourcemap: false,
        target: ['es2015'],
        drop: ['console', 'debugger'],
    },
    compression: {
      gzip: true,
      // BROTLI OPTIONAL
      // brotli: false,
    },
    logLevel: 0, // 0 = logs start / finish; 1 = logs steps; 2 = logs all.
  },
  {{#if testinNutin}}

  // Testing toolkit
  testinNutin: {
    includeInternals: false, // true to run src/core and src/libs tests
    jsdomOptions: {
      runScripts: false,     // or true ("dangerously") if needed
      resources: false,      // or true ("usable") if needed
      freezeGlobals: false,
      pretendToBeVisual: true,
    },
    verbose: false,
  },
  {{/if}}
}
